<div>
    <% if(rs.length > 0) { %>
    <table border="1">
        <!-- &nbsp is HTML syntax for a space it stands for non break space -->
        <tr><th>Name</th><th>Address</th><th>&nbsp;</th></tr>
        <% for (var i=0; rs.length > i; i++) { %>
        <tr>
            <td><a href="/brewery/edit?id=<%= rs[i].id %>"> <%= rs[i].name %></a></td>
            <% var address = rs[i].street + " " + rs[i].city + ", " + rs[i].zip; %>
            <td><%= address %></td>
            <td><a href="/brewery/delete?id=<%= rs[i].id %>&name=<%= rs[i].name %>">Delete</a></td>
        </tr>
        <% } %>
    </table>
    <% } else { %>
    No companies are entered into the brewery database table.
    <% } %>
</div>

<div style="margin-top:10px;">
    <a href="/brewery/create">Create New Brewery</a>
</div>